import Physics, { Entity } from "../Physics";

describe("Physics", () => {
  describe("Physics", () => {
    it("should have some tests", () => {});
  });
  describe("Entity", () => {
    it("should have some tests", () => {});
  });
});
